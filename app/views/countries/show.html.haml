.page-header
  %h1=@country.name
=render 'menu'
-if @films.blank?
  .alert.alert-info Фильмов не найдено
-else
  .list-group
    -@films.each do |film|
      =render 'films/film', film: film
  =paginate @films
